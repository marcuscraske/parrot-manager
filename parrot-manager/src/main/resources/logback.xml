<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{YYYY-MM-dd HH:mm:ss} : %-26thread{26} : %-5level : %-36class{36} : %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.limpygnome" level="debug" additivity="false">
        <appender-ref ref="console" />
    </logger>

    <root level="info">
        <appender-ref ref="console" />
    </root>

</configuration>
